@using Eating2.Business.ViewModels;
@model List<RateViewModel>
@{
    ViewBag.Title = "Trang chủ món ăn";
    //Layout = "~/Views/Shared/_Layout.cshtml";
}


<table class="table table-bordered">
    <tr class="text-center">
        <th style="width:150px", class="text-center">
            <h4>Khách hàng</h4>
        </th >
        <th style="width:80px", class="text-center">
            <h4>Điểm</h4>
        </th>
        <th class="text-center">
            <h4>Nhận xét</h4>
        </th>
        <th style="width:150px", class="text-center">
            <h4>Tác vụ</h4>
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td style="width:auto", class="text-center">
                @Html.DisplayFor(modelItem => item.Customer)
            </td>
            <td style="width:auto", class="text-center">
                @*@{
                    string star = "xxx";

                    for (int i = 0; i < item.Point; i++)
                    {
                        
                    }
                }
                @String.Format( "", "diem")*@
                
                @Html.DisplayFor(modelItem => item.Point)
            </td>
            <td style="width:auto",  class="text-center">
                @Html.DisplayFor(modelItem => item.Comment)
            </td>
            <td class="text-center">

                @Html.ActionLink("Xóa", "Delete", "Rate", new { id = item.ID }, null)
            </td>
        </tr>
                    }

</table>
